# @package _global_
---
defaults:
  - override /model: single_linear_layer
  - override /dm: celeba_male_smiling_small

seed: 1
gpu: 0

model:
  activation: SELU

dm:
  download: false

opt:
  lr: 0.001
  weight_decay: 0.001
